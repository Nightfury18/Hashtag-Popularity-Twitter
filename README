
Models
	This directory contains all LSTM & GRU models and their results. 

Preprocessing
	This directory contains all codes we have written for preprocessing the twitter dump.

Stats
	This directory contains codes which display the statistics of the frequencies of words of every hashtag. 

Train 
	This directory contains codes which prepare the training files for the model.

For more information look at the README in the corresponding directory.
