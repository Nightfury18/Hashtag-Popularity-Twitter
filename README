Directories:

	++ models
		This directory contains all LSTM & GRU models, their results. For more information look at the README in the directory

	++ preprocessing
		All codes we have written for preprocessing the twitter dump. For more information look at the README in the directory

	++ stats
		stats directory contains codes which displays the statistics of frequencies of words of every hashtag. For more information look at the README in the directory

	++ train 
		Train directory contains codes which prepares the training files for the model. For more information look at the README in the directory
